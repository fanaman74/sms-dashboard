SMS_USERNAME=your_parent_email@example.com
SMS_PASSWORD=your_password

# Optional: Home Assistant webhook for summaries
HA_WEBHOOK_URL=

# Optional: push scraped data to a deployed dashboard
# (set both to enable; generate a random secret and use the SAME value on Railway)
INGEST_URL=https://your-app.up.railway.app/api/ingest
INGEST_TOKEN=
